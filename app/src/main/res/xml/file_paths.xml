<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- 对应 getExternalFilesDir() 路径 -->
    <external-files-path
        name="external_files"
        path="." />

    <external-media-path
        name="external_media"
        path="." />

    <external-path
        name="external"
        path="." />

    <!-- 如果需要其他路径可以添加 -->
    <!-- <cache-path name="cache" path="." /> -->
    <!-- <files-path name="files" path="." /> -->
</paths>